type User
    @model
    @auth(
        rules: [
            { allow: owner }
            {
                allow: groups
                groups: ["Administrators"]
                operations: [create, read, update, delete]
            }
        ]
    ) {
    id: ID!
    name: String!
    description: String
    email: String!
    wins: Int!
    loses: Int!
    escapes: Int!
}
